# Host virtualenv (rebuilt inside the image by `uv sync`)
.venv

# VCS / editor / caches
.git
.gitignore
.vscode
__pycache__
**/__pycache__
*.pyc

# Match artifacts
*.pgn
harness/results/

# Native build artifacts (rebuilt in the engines stage; host builds may be
# arch-specific and rust/target is huge)
cpp/build/
rust/target/
