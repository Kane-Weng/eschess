# Copy this file to .env and fill in your values. .env is gitignored.

# Hugging Face access token, used to stream the training dataset without rate
# limits. Create one at https://huggingface.co/settings/tokens (read scope).
HF_TOKEN=
